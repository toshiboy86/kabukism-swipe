import { getFiles } from '@/hooks/utils'
import { consultPlays } from '@/hooks/consult'
describe('Select plays based on users preference', () => {
  test('selected', () => {
    // const playlist = JSON.parse(getFiles('playList/'))
    // consultPlays({like: true, category: 'beginner'}, playlist)
    expect(true).toBeTruthy()
  })
})
